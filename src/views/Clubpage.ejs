<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link href="/Style/Clubpage.css" rel="stylesheet">
    <link href="/Style/general.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/vue@2.7.14/dist/vue.js"></script>
    <script src="/Javascript/Clubpage.js" defer></script>
    <title id="clubtitle"></title>
</head>

<body>
    <div id="app">
        <div id="primary">
            <div class="d-flex">
                <h1 id="webtitle" class="mx-4 my-2">{{name}}</h1>
                <a href="/"><i class="fa fa-home fs-2 mt-2 text-dark" aria-hidden="true"></i></a> 
                <pre class="fs-2 mt-1"> / </pre>
                <a href="/login"><i class="fa fa-user fs-2 mt-2 text-dark" aria-hidden="true"></i></a>
            </div>
            <div id="main" class="col-9 d-block">
                <div id="announs">
                    <h1>Recent Announcements</h1>
                    <div id="anns">
                        <div v-for="ann in announcements" class="ctn ann-box">
                            <div class="a-img"><img src="#" alt="img"></div>
                            <h3>{{ann.title}}</h3>
                            <div>{{ann.announcements}}</div>
                        </div>

                    </div>
                </div>
                <div id="greeting" class="ctn">
                    <h1>Welcome to {{name}}!!!</h1>
                    <p>{{goal}}</p>
                    <img v-bind:src="image" alt="A club's image/logo">
                </div>
            </div>
            <div id="events" class="col-3">
                <h2 class="fw-bold">Upcoming Events</h2>
                <div v-for="e in events" class="event-box"> <!--v-for="event in events" :key="event.id"-->
                    <h3>{{e.event_name}}</h3>
                    <p>{{e.event_description}}</p>
                    <button type="button" class="btn event-details" v-bind:value="e.event_id"
                        v-on:click="show_detail">Interested</button>
                </div>
            </div>
        </div>
        <div id="popup" class="event-details">
            <button type="button" id="close" v-on:click="close_detail" >X</button>
            <h1>{{eDetail.event_name}}</h1>
            <div>TIME: {{eDetail.date_of_event}}</div>
            <div>VENUE: {{eDetail.place}}</div>
            <h3>Description</h3>
            <p>{{eDetail.event_description}}</p>

        </div>
    </div>
</body>

</html>